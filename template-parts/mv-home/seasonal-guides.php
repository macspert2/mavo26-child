<?php
/**
 * Homepage seasonal inspiration — Section 5 (plan-mid.md §4.1).
 *
 * Stub for the next phase: manual or month-driven seasonal list,
 * potentially backed by TVF_Store::query_results() filtered by a
 * seasonal filter_slug (hiver/printemps/ete/automne) once that's wired up.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
